" don't map tab in vimwiki so that UltiSnips can use Tab
let g:vimwiki_table_mappings = 0

" don't change tabs to spaces so I can make tab-separated files
" otherwise keep tab settings in vimrc
setlocal noexpandtab
